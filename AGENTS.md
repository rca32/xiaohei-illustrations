# 저장소 작업 지침

- 설치 단위와 실행 이름은 `xiaohei-illustrations`로 통일한다.
- 문서, 에이전트 설명, 프롬프트, 예제의 기본 언어는 한국어다. 기술 식별자와 원본 라이선스는 유지한다.
- 개인 소개, 연락처, 소셜 계정, 홍보 링크, QR 이미지와 개인명 접두사를 추가하지 않는다.
- `LICENSE`의 원본 저작권·허가 고지를 보존하고 설치 폴더에도 동일하게 동봉한다.
- 예제 원본은 `xiaohei-illustrations/assets/examples/` 한 곳에서만 관리한다. 실제 이미지 생성 도구로 만든 PNG만 AI 생성 결과로 소개한다.
- 샤오헤이의 일관성 참조는 `xiaohei-illustrations/assets/reference/xiaohei-character-reference.png`를 사용하고, 참조 이미지의 장면·소품·배치를 복제하지 않는다.
- 실제 그림 생성은 사용 가능한 이미지 생성 도구로 수행한다. 도구가 없으면 생성했다고 주장하지 않는다.
- 변경 후 `python3 scripts/check_repository.py`를 실행한다. 스킬의 참조 문서는 필요한 것만 읽는다.
- 사용자에게는 변경 내용과 검증 결과, 남은 제한을 한국어로 짧고 쉽게 설명한다.
